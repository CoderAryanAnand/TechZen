var a = 1

class Person
    fun Person(name)
        var this.name = name
    endf
    fun Format()
        return "My name is " + this.name + "."
    endf
end

var inp = input("name: ")
while inp != "exit" then
	var p = Person(inp)
	print(p.Format())
	var inp = input("name: ")
end

exit()
print("This message will never ever show up! Isn't that wacky?!")